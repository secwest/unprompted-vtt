This folder is intentionally committed.

Each export run creates:

  data\<event-slug>\metadata\
  data\<event-slug>\vtt\

The real Zoom credentials belong in the repository root .env file, which is ignored by Git.
